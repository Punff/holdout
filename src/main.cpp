#include <iostream>
#include <raylib.h>
#include <vector>
using namespace std;

int main() {
    InitWindow(800, 600, "raylib");
    SetTargetFPS(60);

    while (!WindowShouldClose()) {
        // Updates
        

        // Draw
        BeginDrawing();
        
        EndDrawing();
    }

    return 0;
}
