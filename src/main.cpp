#include "main.hpp"

int main() {
    InitWindow(800, 600, "raylib");
    SetTargetFPS(60);

    while (!WindowShouldClose()) {
        // Updates
        

        // Draw
        BeginDrawing();
        
        EndDrawing();
    }

    return 0;
}
